export function BackendBootstrap() {
  // Deprecated: keep this component as a no-op for backward compatibility.
  // Dashboard bootstrap now lives in MeContextProvider to prevent duplicate fetches.
  return null;
}

