import { PageHeader } from "@/components/dashboard/page-header";

export default function DeployPage() {
  return (
    <PageHeader
      title="Deploy"
      description="Widget, API keys, and environment targets."
    />
  );
}
