import { PageHeader } from "@/components/dashboard/page-header";

export default function ActionsPage() {
  return (
    <PageHeader
      title="Actions"
      description="Tools and workflows the agent can invoke."
    />
  );
}
